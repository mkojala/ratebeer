class Beer < ActiveRecord::Base
 belongs_to :brewery
  has_many :ratings

	def average_rating
		sum = 0
		ratings.each do |r| 	
			sum = sum + r.score
		end
		sum / ratings.count.to_f
	end

	def display_beer_option_text
		"#{name} - #{brewery.name}"
	end
end
